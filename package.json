{
  "name": "app-tasks",
  "private": true,
  "scripts": {
    "apifake": "json-server --watch db.json --port 3001",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18.17.3",
    "@vue/cli-service": "^5.0.8",
    "json-server": "^0.17.3",
    "nuxt": "^3.7.3",
    "vuetify-nuxt-module": "^0.5.7"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}
